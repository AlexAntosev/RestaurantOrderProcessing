@model RestaurantOrderProcessing.WebUI.Models.RegisterViewModel

@{
    ViewBag.Title = "Register";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="order-main-text">Регистрация:</h2>
@using (Html.BeginForm("Register", "Account", new { Model }))
{
<div class="checkout-center">
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()
    <div class="form-group">
        <h4 class="text-center">Email:</h4>
        @Html.TextBoxFor(x => x.Email, new { @class = "form-control textbox-bank-card checkout-center" })
    </div>
    <div class="form-group">
        <h4 class="text-center">Пароль:</h4>
        @Html.TextBoxFor(x => x.Password, new { @class = "form-control textbox-bank-card checkout-center" })
    </div>
    <div class="form-group">
        <h4 class="text-center">Подтвердите пароль:</h4>
        @Html.TextBoxFor(x => x.ConfirmPassword, new { @class = "form-control textbox-bank-card checkout-center" })
    </div>
    <div class="text-center">
        <input class="btn btn-primary" type="submit" value="Подтвердить" />
    </div>
</div>
}



